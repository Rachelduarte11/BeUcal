import 'package:flutter/material.dart';

class Asistente extends StatelessWidget {
  const Asistente({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}