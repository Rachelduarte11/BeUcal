import 'package:flutter/material.dart';

class ND2Performance extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Text("Se muestra contenido de ND2Performance",  style: TextStyle(
            fontSize: 20,
            color: Colors.black,
            fontFamily: "Arimo"
          )
        ),
      ],
    );
  }
}