import 'package:flutter/material.dart';

class AnalisisInventario extends StatelessWidget {
  const AnalisisInventario({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}