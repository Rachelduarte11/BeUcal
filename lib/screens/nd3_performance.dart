import 'package:flutter/material.dart';

class ND3Performance extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [
        Text("Se muestra contenido de ND3Performance",
          style: TextStyle(
            fontSize: 20,
            color: Colors.black
          )
        ),
      ],
    );
  }
}